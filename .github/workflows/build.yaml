name: Moped Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: mxcl/xcodebuild@v1

#- name: mxcl/Xcodebuild
#  uses: mxcl/xcodebuild@v1.7.0
